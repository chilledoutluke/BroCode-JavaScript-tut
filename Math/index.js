// Math = built-in object that provides a collection 
// of properties(PI and E) and methods e.g. .round() 

let x = 3;  //3.99 for other examples
let y = 2;
let z = 1;

//z = Math.round(x); // rounds up
//z = Math.floor(x); // rounds down 
//z = Math.ceil(x); // Always rounds up, opposite of .floor()
//z = Math.trunc(x); // Will eliminate any decimal portion
//z = Math.pow(x, y); // Power of
//z = Math.sqrt(x); // Square Root
//z = Math.log(x);// Natural logarithm of a number
//z = Math.sin(x); // Sine function
//z = Math.cos(x); // cosine function
//z = Math.tan(x); // tangent function
//z = Math.abs(x); //changes negative to positive
//z = Math.sign(x); // either gives 1. -1 or 0
//let max = Math.max(x, y, z); // gives the max
//let min = Math.min(x, y, z); // gives the min
console.log();